CREATE TABLE %s (
    id int NOT NULL AUTO_INCREMENT,
    `user_id` INT NULL ,
    `payment_method` VARCHAR(45) NULL ,
    `payment_details` text NULL ,
    `min_payout` INT NULL,
    `affiliate_plan` varchar(45) NOT NULL DEFAULT '1',
    PRIMARY KEY  (id)
);